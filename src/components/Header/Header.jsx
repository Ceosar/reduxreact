import React from "react";
import classes from "./Header.module.css"

const Header = () => {
    return (
        <header className={classes.header}>
            <img width={60} alt="icon"/>
        </header>
     );
}

export default Header;